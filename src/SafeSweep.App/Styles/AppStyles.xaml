<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:conv="clr-namespace:SafeSweep.App.Converters">

    <conv:BytesConverter x:Key="Bytes" />
    <conv:LevelToTextConverter x:Key="LevelText" />
    <conv:RiskToTextConverter x:Key="RiskText" />
    <conv:RiskToBrushConverter x:Key="RiskBrush" />
    <conv:MethodToTextConverter x:Key="MethodText" />
    <conv:BoolToVisibilityConverter x:Key="BoolVis" />
    <conv:BoolToVisibilityConverter x:Key="InvBoolVis" Invert="True" />
    <conv:NullToVisibilityConverter x:Key="NullVis" />
    <conv:NullToVisibilityConverter x:Key="InvNullVis" Invert="True" />
    <conv:CountToVisibilityConverter x:Key="CountVis" />
    <conv:CountToVisibilityConverter x:Key="InvCountVis" Invert="True" />
    <conv:InverseBoolConverter x:Key="InvBool" />

    <!-- Typography -->
    <Style x:Key="PageTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="28" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
    </Style>

    <Style x:Key="PageSubtitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Margin" Value="0,4,0,0" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
        <Style.Triggers>
            <!-- Short windows: keep the room for content; the title still says where you are. -->
            <DataTrigger Binding="{Binding DataContext.IsCompactHeight, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Secondary explanatory text that gives way on short windows. -->
    <Style x:Key="CompactHidden" TargetType="FrameworkElement">
        <Style.Triggers>
            <DataTrigger Binding="{Binding DataContext.IsCompactHeight, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SectionTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Margin" Value="0,0,0,8" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
    </Style>

    <Style x:Key="Muted" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
    </Style>

    <Style x:Key="BigNumber" TargetType="TextBlock">
        <Setter Property="FontSize" Value="30" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
    </Style>

    <Style x:Key="Body" TargetType="TextBlock">
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
    </Style>

    <!-- Surfaces -->
    <Style x:Key="CardBorder" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="16" />
    </Style>

    <!-- Only shown on short windows (small spacers that replace hidden text). -->
    <Style x:Key="CompactOnly" TargetType="FrameworkElement">
        <Setter Property="Visibility" Value="Collapsed" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding DataContext.IsCompactHeight, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                <Setter Property="Visibility" Value="Visible" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- A card that gives way on short windows (the same numbers are on the dashboard). -->
    <Style x:Key="CompactHiddenCard" TargetType="Border" BasedOn="{StaticResource CardBorder}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding DataContext.IsCompactHeight, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Badge" TargetType="Border">
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Padding" Value="8,2" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="PagePadding" TargetType="Grid">
        <Setter Property="Margin" Value="28,20,28,20" />
    </Style>

    <!-- Toolbar spacing -->
    <Style x:Key="ToolbarPanel" TargetType="WrapPanel">
        <Setter Property="Margin" Value="0,12,0,12" />
    </Style>

</ResourceDictionary>
